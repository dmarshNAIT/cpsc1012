using System;

namespace Oct1Formatting
{
    class Program
    {
        static void Main(string[] args)
        {
            // declare variables
            int num, numFactorial, count;

            // prompt user
            Console.WriteLine("give me a number:");
            num = int.Parse(Console.ReadLine());

            // declare initial values for loop
            count = num;

            // run our loop until we get to 1
            while (count > 1)
            {
                // multiply count * (count -1 )
                // decrement 

            }

            // display results



            Console.ReadLine();
        }
    }
}
